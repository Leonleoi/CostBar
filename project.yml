name: AIUsageTracker
options:
  bundleIdPrefix: com.leonlei
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  AIUsageTracker:
    type: application
    platform: macOS
    info:
      path: Info.plist
      properties:
        LSUIElement: true
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
    entitlements:
      path: AIUsageTracker.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.network.client: true
        com.apple.security.files.user-selected.read-write: true
        com.apple.security.personal-information.keychain: true
    sources:
      - path: AIUsageTrackerApp.swift
      - path: Views
      - path: ViewModels
      - path: Models
      - path: Services
      - path: Storage
      - path: Utils
      - path: Assets.xcassets
    settings:
      base:
        INFOPLIST_FILE: Info.plist
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "-"
        PRODUCT_BUNDLE_IDENTIFIER: com.leonlei.aiusagetracker
        ENABLE_HARDENED_RUNTIME: true
    dependencies: []

  AIUsageTrackerTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: AIUsageTrackerTests
        type: group
    dependencies:
      - target: AIUsageTracker
    settings:
      base:
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/AIUsageTracker.app/Contents/MacOS/AIUsageTracker"
        BUNDLE_LOADER: "$(TEST_HOST)"
