name: kx
options:
  bundleIdPrefix: com.leonlei
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  kx:
    type: application
    platform: macOS
    info:
      path: Info.plist
      properties:
        LSUIElement: true
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
    entitlements:
      path: kx.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.network.client: true
        com.apple.security.files.user-selected.read-write: true
        com.apple.security.personal-information.keychain: true
    sources:
      - path: kxApp.swift
      - path: Views
      - path: ViewModels
      - path: Models
      - path: Services
      - path: Storage
      - path: Utils
      - path: Support
      - path: Assets.xcassets
    settings:
      base:
        INFOPLIST_FILE: Info.plist
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "-"
        PRODUCT_BUNDLE_IDENTIFIER: com.leonlei.kx
        ENABLE_HARDENED_RUNTIME: true
    dependencies: []

  kxTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: kxTests
        type: group
    dependencies:
      - target: kx
    settings:
      base:
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/kx.app/Contents/MacOS/kx"
        BUNDLE_LOADER: "$(TEST_HOST)"
